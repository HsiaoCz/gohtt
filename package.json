{
  "name": "gohtt",
  "version": "1.0.0",
  "description": "how to install tailwindcss npm install -D tailwindcss npx tailwindcss init",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tailwindcss build -o public/styles.css --minify",
    "watch": "tailwindcss build -o public/styles.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.3"
  }
}
